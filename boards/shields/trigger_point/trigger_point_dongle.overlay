/ {
    chosen {
        zmk,split-role = "central";
        zmk,matrix-transform = &mt;   /* central doesn't have kscan; use a transform */
    };

    mt: matrix_transform {
        compatible = "zmk,matrix-transform";
        /* set these to match your keyboard matrix */
        rows = <5>;
        columns = <14>;
    };
};