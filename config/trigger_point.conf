# Standalone, no split:
# (comment out or delete any previous split lines)
# CONFIG_ZMK_SPLIT is intentionally omitted
CONFIG_ZMK_KEYBOARD_NAME="Trigger Point"
CONFIG_ZMK_USB=y
# (leave BLE off for this test to keep it simple)
