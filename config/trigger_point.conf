# Common minimal settings
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_KEYBOARD_NAME="Trigger Point"

# Optional but harmless
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# Basic logging (valid symbols only)
CONFIG_LOG=y
